# mlm-web

Frontend приложения **mlm-web** (Next.js).  
Репозиторий / локальная папка: `almat-mlm-web`.

Канон: submodule `docs/` → `almat-mlm-docs`. См. `AGENTS.md`, `docs/TECH_SPEC.md` §4.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
